language: python
python:
  - "3.6"      # current default Python on Travis CI

# command to install dependencies
install:
  - pip install pipenv
  - pipenv install
# command to run tests
script:
  - ci-scripts/run_bowser_tests.sh
  - ci-scripts/run_bowser.sh
  - ci-scripts/test-spider.sh
